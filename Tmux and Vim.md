# What is Tmux?
https://en.wikipedia.org/wiki/Tmux

Once you ssh into your server, you can start a tmux session, and until you terminate it, you can always connect to that
same session - it's persistent. This means, dropping your ssh connection does not terminate the tmux session.

- 