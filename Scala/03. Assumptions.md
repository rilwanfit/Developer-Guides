#Assumptions

in addition to default three assertions, we can use the features withing the Assertion trait.
- This trait provide methods that can cancel a test if some assumptions are not met.
- tests cancelled not failed :)

``` assume(database.isAvailable(), "Duh!") ```